# ESTADO — ERP Aurelle

> Bastón de relevo entre sesiones. Se SOBREESCRIBE (no se acumula). Máx. ~1 página.
> Última actualización: 2026-07-12 (Claude Code, sesión de Fer) — **PR #38 (bugfix loop del bot + CRM leads) MERGEADO y desplegado; 0 PRs abiertos, todo el trabajo de todas las sesiones vive en el tronco.** **Migraciones 0004–0037 TODAS aplicadas en prod (0037 confirmada por Fer 2026-07-12) — no queda SQL pendiente.** Meta verificó el negocio, se conectó todo en vivo (env vars + redeploy + app publicada) y la prueba de fuego pasó: mensaje real → ficha sola → bot respondió. Misma sesión: **widget flotante de Inbox global**, **reglas anti-redacción-IA + cero emojis + delay humanizado con typing indicator** en el bot. Siguiente frente grande: **módulo de follow-ups/cadencias** (spec de Santiago recibido; ver "Siguiente tarea").

## Fase actual
**Fase 3 — Riel vivo de WhatsApp: OPERANDO EN PRODUCCIÓN (2026-07-11).** Conectado en sesión en vivo con Fer: Meta verificó el negocio, app "Aurelle ERP" **publicada (Activo)**, las 4 env vars pegadas en Vercel (`WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`=1302482549604139 del +52 2206289675, `WHATSAPP_APP_SECRET`, `ANTHROPIC_API_KEY` con cuenta org "Aurelle & Co." + $10 USD) + redeploy. **Prueba de fuego PASADA:** mensaje real → conversación + ficha creadas solas → bot respondió por la API. Falta del plan original: **probar 2-3 días y re-apuntar los ads al número nuevo** (§"El corte de los ads" de la guía) y borrar al final el cliente de prueba de Fer ("SRR"). El riel (webhook + Cloud API + bot IA + cola de aprobación) está mergeado al tronco (PR #4) y desplegado; `/api/webhook/whatsapp` responde en prod. **Sesión de conexión con Santiago (2026-07-08), avance real:** app de Meta "Aurelle ERP" creada (solo caso de uso WhatsApp; IG/Messenger al backlog), asociada al Business Manager de los ads. Webhook **verificado ✓** con `WHATSAPP_VERIFY_TOKEN` (ya en Vercel + redeploy hecho) y campo `messages` suscrito. Número de la **eSIM registrado en la Cloud API: +52 2206289675**. **Token permanente generado** (System User). Permisos `whatsapp_business_messaging`/`management`/`public_profile` listos. Aviso de privacidad público en `/privacidad` (en prod, para publicar la app). **BLOQUEO:** Meta pide **verificación del negocio** (subir documentos, ~2 días hábiles) antes de entregar webhooks/mensajes de producción. Mientras no verifique + se publique la app, el Inbox no recibe mensajes reales aunque WhatsApp marque ✓✓. **Pendiente al desbloquear (retomar rápido):** pegar en Vercel `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID` (del +52 nuevo), `WHATSAPP_APP_SECRET`, `ANTHROPIC_API_KEY` → redeploy → publicar app (switch a Activo, con la liga de privacidad) → prueba de fuego (escribir al número → ver en `/clientes/inbox`) → re-apuntar ads al número nuevo. Detalle en `docs/GUIA_WHATSAPP.md` §"Dónde nos quedamos". **Fase 4 CERRADA**. **Fase 5 en curso** (tareas sugeridas 0027/0028, resultado de cita→pipeline). **Marketing v1+v2 (Fase 6 adelantada, 0029):** funnel por fuente **y por campaña/zona** + gasto manual → CAC (por canal y por campaña) en `/crecimiento`. **Dashboard por rol (§3.16):** `/hoy` con toggle Ventas/Taller. **Tareas sugeridas aceptar/descartar (§3.15, 0031)** + **cita→tarea de preparación** + **tareas recurrentes (§3.15, 0032)** en `/hoy/tareas/recurrentes`. **Postventa v1 (Fase 6, 0030):** al entregar → registro de pieza (garantía+aniversarios), servicios, y `/clientes/postventa` con métrica de recompra. Fase 1 y núcleo de Fase 2 completos y en prod.

## Hecho
- **Fase 0** completa (app, diseño, navegación, roles+RLS+auditoría). Supabase + Vercel arriba.
- **Fase 1 completa** — Clientes (0004), Inventario+costos solo-admin (0005/0006), Cotizador+margen solo-admin (0007), Pedidos: candado anticipo 2 + pagos + reserva + costo/margen real + conversión (0008), Tareas+Dashboard "Hoy" (0009). Cada uno con su `docs/modulos/*.md`.
- **Fase 2 en curso** — Finanzas v1 (0010/0011): ledger solo-admin, asiento automático al pagar, CxP a consignante al reservar, captura manual, P&L del mes, capital de trabajo. Proveedores v1 (0012): directorio en `/dinero/proveedores`. **Gastos recurrentes (0013):** alta + posteo mensual idempotente (función SECURITY DEFINER) + Vercel Cron protegido + botón manual; burn fijo en `/dinero/gastos`. Tercer evento §4 automático. **Compras a proveedor v1 (0014):** registrar compra en `/dinero/compras` → asiento (costo/gasto) + CxP a crédito (4º evento §4). **Proyección de flujo 30/60/90** en `/dinero`. **Comisiones v1 (0015):** `/dinero/comisiones`, % sobre utilidad real. **Reporte al socio:** `/imprimir/reporte-socio`. **Alta de items desde la compra (0016):** una compra de inventario da de alta la pieza + su costo, ligada a la compra. `docs/modulos/finanzas.md`, `docs/modulos/gastos-recurrentes.md`, `docs/modulos/compras.md`, `docs/modulos/comisiones.md`.
- **Producción verificada:** las 19 tablas y el esquema de `tarea` (`entidad_tipo`/`estado`) confirmados en el Supabase de Santiago; migraciones 0004–0015 aplicadas. La bifurcación de dos ramas paralelas (2026-07-06) quedó reconciliada; el tronco oficial es `claude/business-erp-plan-kqb9uk`.
- **Fase 3 (sin Meta) hecho** — **Citas (0017)** (agenda showroom, candado anti doble-reserva, resultado=funnel), **envío asistido** (`BotonWhatsApp` en Pedido/Cita, wa.me), **Inbox shell (0018)**, **Fechas importantes** (`/clientes/fechas` + tarjeta en `/hoy`). `docs/modulos/{citas,mensajeria,inbox}.md`.
- **Fase 4 — Producción** (0019, v2 responsable+QC, **v3 checklist QC editable 0023** en `/taller/produccion/qc`, **v4 consumo de material + QC→cita entrega 0026**): kanban, avanzar en 2 toques, QC con candado, **costos→`costo_real` del pedido** (trigger). **v4 (2026-07-09):** tarjeta "Materiales del pedido" en la orden → **consumir item reservado** (pasa a `consumido`, su costo suma al `costo_real`, migración 0026 SECURITY DEFINER); **QC completo → tarea sugerida "Agendar cita de entrega"** (idempotente, en `/hoy`). Cierran las 2 filas §4 no-Meta que faltaban. Validado en Postgres local + build/lint + smoke (`op1` muestra la tarjeta con "Marcar consumido"). `docs/modulos/produccion.md`.
- **Fase 4 — Documentos** (0020 e-firma pública `/firmar/[token]`, **v3 snapshot congelado 0022**, **v4 cláusulas editables 0024** en `/sistema/contrato`): contrato/recibo imprimibles, contrato automático al confirmar, ficha 360 (tabs Documentos/Media), firma en canvas. Al firmar se congela `documento.contenido` (ContratoDatos + cláusulas) → un contrato firmado no cambia aunque cambie el pedido; `<ContratoDoc>` cae al default si faltan. `docs/modulos/documentos.md`.
- **Fase 4 — Biblioteca de media (0021):** galería `/taller/biblioteca` + renders/fotos en el pedido (bucket privado `media`, URLs firmadas 1h, versionado); enganches: envío por WhatsApp asistido, foto por etapa desde Producción, render aprobado→orden a `aprobacion_cliente`. `docs/modulos/biblioteca-media.md`.
- **Fase 4 — Cron nocturno de seguimiento (0025):** `/api/cron/seguimiento` (Vercel Cron diario, `CRON_SECRET`) → función idempotente `generar_tareas_seguimiento`: tarea automática por atasco de producción ≥7d y por contrato sin firmar >48h. `docs/modulos/seguimiento-cron.md`.
- **Todas las features validadas** en Postgres local (cadena 0001–0030) + build/lint verdes + smoke test. **Guardarraíl anti-bifurcación:** hook `SessionStart` imprime ESTADO y lista ramas al arrancar.
- **Sesión 2026-07-09 (larga, sin Meta):** (a) **Fase 4 cerrada** — consumo de material (0026) + QC→cita de entrega. (b) **Fase 5** — tareas sugeridas por IA: cotización sin respuesta (0027) y stock bajo (0028, con `/taller/inventario/umbrales`); **resultado de cita→avanza pipeline + no-show→tarea de reagendar**; **dashboard por rol** (`/hoy` toggle Ventas/Taller). (c) **Marketing v1 (0029)** — funnel por fuente + gasto→CAC en `/crecimiento`. (d) **Postventa v1 (0030)** — registro al entregar (garantía+aniversarios) + servicios + `/clientes/postventa` (recompra). (e) **Borrar cliente** desde la ficha (solo-admin, sin SQL). (f) **Bugfix:** "citas de hoy" ancladas a Monterrey (antes se corrían por UTC). `docs/modulos/{produccion,inventario,seguimiento-cron,citas,tareas,marketing,postventa,clientes}.md`.
- **Sesión 2026-07-11 (sin Meta, "todo lo que se pueda"):** (a) **Aceptar/descartar sugerencias en un toque (§3.15, 0031)** — badge "Sugerida" + Aceptar/Descartar en `/hoy`; descartar = columna booleana `tarea.descartada` (no rompe la idempotencia del cron). (b) **Cita agendada → tarea sugerida de preparación** (`crearTareaPreparacion`, app-level, idempotente por cliente). (c) **Tareas recurrentes (§3.15, 0032)** — plantilla `tarea_recurrente` (semanal/mensual) + generador idempotente por período, UI en `/hoy/tareas/recurrentes`; el cron nocturno llama las 3 funciones (seguimiento + recurrentes + postventa) por RPC. (d) **Postventa v2 (§3.7, 0033)** — `generar_tareas_postventa`: garantía por vencer ≤30d + aniversario entrega/boda ≤14d (match mes-día sin bug 29-feb). (e) **Marketing v2 (§3.13)** — `funnelPorCampana`: funnel + CAC por campaña/zona (`fuente_detalle`) en `/crecimiento`. (f) **Expos (§3.14, 0034)** — módulo nuevo `/crecimiento/expos` (solo-admin): calendario+estado+costo, captura de lead en el stand (→ cliente fuente=expo) y **ROI por evento** (leads→visitas→cierres→ingreso, ROI%, CAC) casando `fuente_detalle↔expo.nombre`. `docs/modulos/{tareas,recurrentes,postventa,marketing,expos}.md`. Validado: cadena local 0001–0034 + build/lint verdes + smoke de ROI (3 leads→2 cierres→$140k→ROI 180%).
- **Marketing v3 — sync de gasto Meta Ads (2026-07-11, sin migración):** `lib/meta-ads.ts` + `lib/data/ads-sync.ts` (upsert por periodo+canal+campaña) + cron `/api/cron/ads` (8:30, CRON_SECRET) + botón "Sincronizar con Meta" en `/crecimiento`. Inerte hasta poner `META_ADS_ACCESS_TOKEN`/`META_AD_ACCOUNT_ID` en Vercel. `docs/modulos/marketing.md`.
- **Contactos útiles (0036, 2026-07-11, pedido de Santiago):** directorio para tercerizar procesos en `/taller/contactos` (joyeros/vaciadores/montadores: contacto, especialidad, tiempo de entrega, precio estimado — todo opcional salvo nombre). Visible para todos (no solo-admin; distinto de `proveedor`/Dinero). Tarjeta en el índice de Taller. Validado: cadena local 0001–0036 + build/lint + smoke con captura. `docs/modulos/contactos-utiles.md`.
- **Sesión 2026-07-11 (conexión + humanización, con Fer):** (a) **Conexión Meta completa y prueba de fuego pasada** (riel VIVO en prod). (b) **Widget flotante de Inbox** en toda la app (`InboxFlotante` + `/api/inbox/resumen`, polling 25 s, Notification API). (c) **Bot humanizado:** prompt anti-redacción-IA, cero emojis (prompt + filtro), delay 15-30 s/5-12 s con typing indicator (`indicarEscribiendoWa`), `maxDuration=60`; "¿eres bot?"→sensible. (d) Textos desactualizados del Inbox corregidos. `docs/modulos/{inbox,bot-ia}.md`.
- **Bugfix bot (2026-07-11, sesión aparte, sin migración):** con un borrador de horario sin aprobar, cada mensaje nuevo del cliente generaba OTRO borrador y el bot dejaba de contestar hasta que alguien aprobara. Ahora, con `borrador_ia` pendiente en el hilo, el bot SIGUE conversando completamente normal (info, preguntas — ajuste v2 de Santiago); lo único en pausa es proponer horario ("te confirmo en un momento" si preguntan directo). Al aprobar/descartar vuelve el flujo normal. **Observar en prod** (requiere IA real). `docs/modulos/bot-ia.md`.
- **Bifurcación de contactos reconciliada (2026-07-12):** la sesión CRM y otra sesión construyeron "contactos útiles" en paralelo (la primera se colgó y Santiago relanzó el pedido). Se adoptó la versión mergeada del tronco (0036, PR #39), el duplicado se eliminó y la migración CRM se renumeró **0036→0037_lead_interes_telefonos** (renombrada en PR #38). Ver DECISIONES 2026-07-12.
- **Tanda CRM de Santiago (2026-07-11, misma sesión, migración 0036):** (a) **Lista de leads at-a-glance** en `/clientes`: qué busca el lead (`cliente.interes`, lo escribe el bot con cada mensaje), última interacción, blurbs "N sin contestar" / "Respuesta por aprobar", contacto principal con icono, **chips de filtro por etapa con conteo** y **override manual de etapa en la fila** (para Fer, sin abrir ficha). (b) **Intake multicanal:** `correo`/`instagram`/`facebook`/`otro_contacto` + `contacto_preferido` (el principal = lo único visible en listas; el resto solo en ficha); mínimo un contacto obligatorio (zod, no CHECK); edición en tarjeta "Datos de contacto" del Resumen; captura de expo acepta teléfono/correo/otro. (c) **Teléfonos sin espacios** (normaliza alta/riel/expo + migración para existentes) y **búsqueda insensible a espacios** (solo-dígitos ≥4). (d) **Ficha viva del bot:** `resumen_interes` + `nombre_cliente` (actualiza nombre cuando el cliente dice el suyo; nunca el de la pareja, no degrada nombres completos). Validado: cadena 0001–0036 en Postgres local (+ smoke de normalización/colisión/constraint) + build/lint + capturas desktop/móvil. `docs/modulos/{clientes,bot-ia}.md`.
- **Fase 3 — Riel vivo de WhatsApp (2026-07-08, sin migración):** (1) **webhook** `/api/webhook/whatsapp` (verify GET + POST con firma; público en el middleware); (2) **`src/lib/whatsapp.ts`** (Cloud API: enviar texto/plantilla, marcar leído, validar firma, parsear payload); (3) **`inbox-riel.ts`** (service_role: dedup por wa_id, find-or-create cliente+conversación por últimos 10 dígitos, estados de salientes); (4) **bot con IA** (`src/lib/ia/`, `@anthropic-ai/sdk`, `claude-opus-4-8`, structured output): clasifica+redacta, sensible→borrador de aprobación, seguro→envía; corre en `after()`; (5) **acciones del Inbox** (`responderInbox`, `aprobar/descartarBorrador`, `marcarLeido`) + componentes; el hilo usa API si el riel está configurado, si no cae al asistido. **Middleware:** `/api/webhook` y `/api/cron` ahora públicos (destapó un bug latente de los crons). Webhook probado local (verify 200/403, POST 200); bot completo se verifica en prod. `docs/modulos/inbox.md`, `docs/modulos/bot-ia.md`, `docs/GUIA_WHATSAPP.md`.

## Migraciones en prod: **0004–0037 TODAS aplicadas** (0036 y 0037 confirmadas por Fer el 2026-07-12; no queda SQL pendiente)
- 0031–0034 aplicadas por Santiago el 2026-07-11 (aceptar/descartar, recurrentes, cron postventa, Expos). Ya operan en prod: badge Sugerida en `/hoy`, `/hoy/tareas/recurrentes`, cron nocturno de garantías/aniversarios, y **`/crecimiento/expos`** (botón "Expos y ROI").

## Siguiente tarea exacta
**Módulo de follow-ups/cadencias: DISEÑO COMPLETO LISTO en `docs/DISENO_FUNNEL_MENSAJES.md` (sesión de diseño con Fer 2026-07-12; el xlsx de 23 plantillas NUNCA existió — este doc lo sustituye). Decisiones §7 RESPONDIDAS (tiempos OK; incentivos = sesión aparte pendiente; F2 OK; informe semanal por WhatsApp 1:1 a socios — la API NO escribe en grupos). **Requisito nuevo de Fer: SIMULADOR de leads falsos en tiempo acelerado (§8 del diseño) — F1 y simulador se construyen JUNTOS y no se sale en vivo sin batería en verde; meta: vivos la próxima semana (§9).** Spec original de Santiago en `docs/SPEC_FOLLOWUPS.md` (sigue vigente como marco). Fase 1 semiautomática: máquina de estados del lead (NUEVO/CALIENTE/CITADO/COTIZADO/NO_ASISTIO/FRIO/CLIENTE/DESCARTADO), cadencias de toques por estado, pantalla "toques de hoy" con mensaje listo, kanban por estado, regla de oro (entrante pausa/reinicia cadencia), escalamiento 2ct+. **Decisión de arquitectura tomada (ver DECISIONES): NO tabla `lead` paralela — extiende `cliente` (pipeline ya existe) + tabla `cadencia_toque`.** Santiago tiene un xlsx con las 23 plantillas (pestaña CADENCIAS): pedírselo para el seed. Antes de codear: proponer a Santiago (a) esquema, (b) pantalla toques-de-hoy, (c) flujo del mensaje entrante — él aprueba lo funcional, no lo técnico.
**Frente nuevo (2026-07-12): MIGRACIÓN de las 4 sheets al ERP — plan completo en `docs/MIGRACION_DATOS.md`** (fuentes identificadas en el Drive con acceso ya conectado; 3 fases: escaneo/paquete → conciliación con socios → import idempotente por lotes con dry-run local y confirmación de Santiago). **Pendientes chicos previos:** (1) **Activar el sync de Meta Ads (construido 2026-07-11, Marketing v3):** Fer/Santiago deben poner en Vercel `META_ADS_ACCESS_TOKEN` (System User + permiso `ads_read` sobre la cuenta publicitaria) y `META_AD_ACCOUNT_ID` (el `act_…`) + redeploy → el gasto por campaña llega solo (cron 8:30 + botón en /crecimiento). (2) Re-apuntar ads al número nuevo — Fer quiere esperar hasta estar contento con cómo contesta el bot. (3) Borrar cliente de prueba "SRR" al final. (4) **Plantillas de Meta: REDACTADAS (tanda 1 en `docs/PLANTILLAS_META.md`, 2026-07-12) — Fer las sube al WhatsApp Manager vía sesión de Cowork (prompt entregado); al aprobarse, cablearlas con `enviarPlantillaWa`.** Sin ellas la Fase 2 de cadencias no puede automatizar toques fuera de la ventana de 24 h.

**Candidatos de CÓDIGO no bloqueados (los 1–5 de la tanda pasada YA se construyeron esta sesión):**
- ✅ Aceptar/descartar sugerencias (0031) · ✅ Cita→tarea de preparación · ✅ Funnel v2 por campaña · ✅ Expos §3.14 (0034) · ✅ Postventa v2 cron (0033) · ✅ Tareas recurrentes (0032). Todo mergeado y validado local; **falta que Santiago corra los SQL 0031–0034 en prod.**
- **Aún sin construir (sin Meta):** **Base de conocimiento (§3.18)** · **Programa de referidos** · **filtro por mes en el funnel** · ingreso por servicios de postventa → Finanzas / costo de expo → asiento en el ledger · recordar la vista Ventas/Taller por usuario (§3.16 v2) · **pruebas automatizadas** (hoy verificación = build + Postgres local + smoke).
El lifecycle que ENVÍA por WhatsApp, bot v2, media entrante e IG/Messenger esperan el riel vivo (Meta).

Luego (código, ya con el riel vivo y datos fluyendo):
1. **Bot v2 (Fase 5):** que el bot RESERVE cita en Citas y precargue el cotizador desde la conversación (hoy solo invita a agendar). Matriz §4: "cita reservada por bot".
2. **Media entrante:** bajar imágenes/certificados del cliente a Storage (hoy se guarda el mensaje sin el binario).
3. **Plantillas de Meta** pre-aprobadas para escribir fuera de la ventana de 24h (recordatorio de cita, felicitación) → cablear a Citas/Fechas/Pagos con `enviarPlantillaWa` (ya existe).
4. **Persona/tono del bot editable** por Santiago sin tocar código (hoy el prompt vive en `bot-whatsapp.ts`).
5. **Inbox omnicanal: Instagram DM + Messenger (pedido por Santiago 2026-07-08).** Sumar los DMs de IG y Messenger al mismo Inbox/CRM. La app de Meta "Aurelle ERP" ya existe y admite agregar esos casos de uso después sin rehacer WhatsApp. Requiere: webhook propio por canal (payload distinto al de WhatsApp), campo `canal` en `conversacion` (hoy implícito WhatsApp), casar identidad IG/FB↔cliente (no hay teléfono: nuevo identificador), y app review de Meta para permisos de mensajería de IG/Messenger. Después de que el riel de WhatsApp esté estable en prod.
Patrón: migración → Postgres local → dominio → datos+muestra → acciones → páginas → docs.


## Reacciones de la matriz §4 (estado)
Implementadas: pago→ingreso (0010), consignación→CxP (0011), gasto recurrente→asiento mensual (0013), compra→asiento+CxP+alta de item (0014/0016), costo_produccion→costo_real (0019), etapa de producción→estado del pedido (0019, `sincronizarPedido`), **render aprobado→orden a aprobacion_cliente (0021, en `alternarAprobado`)**, QC como candado a listo_entrega, **contrato automático al confirmar el pedido (Documentos v2, `crearContratoSiNoExiste`)**, firma→snapshot congelado del contrato (0022), **atasco de producción→tarea y contrato sin firmar→tarea (cron nocturno, 0025)**, **mensaje entrante de WhatsApp→crea/actualiza cliente+conversación e IA clasifica; sensible→cola humana (riel vivo, `inbox-riel.ts` + `bot-whatsapp.ts`)**, **pieza consumida→inventario a consumido + su costo suma al costo_real (0026, `consumirMaterial`)**, **QC completo→tarea sugerida de cita de entrega (0026 en dominio, `sugerirCitaEntrega`)**, **cotización enviada sin respuesta ≥5d→tarea de seguimiento (cron nocturno, 0027)**, **resultado de cita→avanza pipeline del cliente (solo hacia adelante) y no-show→tarea de reagendar (`registrarResultado`)**, **stock bajo→tarea de recompra (0028, umbral por tipo + cron nocturno)**. Pendientes (fases posteriores): Postventa/Comisiones/lifecycle al entregar (Fase 6); notificación push (QC completo, etc.); resto de tareas sugeridas por IA (conversación fría, requiere riel) + Dashboard por rol.

## Problemas conocidos / bloqueos
- **Sandbox inestable para `npm run start` (2026-07-08):** el servidor de prod local se puede caer (SIGTERM, exit 144) tras varios `build`+`start`. **La mitigación FUNCIONA:** `rm -rf .next && npm run build` una vez y un solo `npm run start`; así se corrió el smoke test de Documentos v2 el mismo día. Para Playwright: instalar `playwright-core` en el scratchpad (no en el repo) y usar `executablePath` `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`.
- El sandbox de Claude no alcanza el Supabase/Vercel de Santiago (política de red). Verificación: build + Postgres local + `npm run start` con datos de muestra; producción se confirma con Santiago vía queries.

- **Migraciones en prod: 0004–0030 TODAS aplicadas** (0026–0030 el 2026-07-09) + `CRON_SECRET` + bucket privado `media`. La 0022 quedó idempotente. El riel de WhatsApp NO trae migración (usa 0018).
- **Env vars del riel: TODAS en Vercel desde 2026-07-11** (bot y envío por API operando). El delay humanizado + typing y el widget flotante de esta sesión se validaron con build+lint+smoke local; **su comportamiento en prod real (delay visible, notificaciones) hay que observarlo estos días de prueba**.
- **Auditoría 2026-07-09 (segunda pasada): sin hallazgos graves; menores de la auditoría anterior RESUELTOS** — (1) comentarios obsoletos de pagos/reserva corregidos (los asientos corren por triggers 0010/0011); (2) `alternarAprobado` endurecido: `pedido_id`/`tipo` se leen de la fila `media`, ya no del navegador; (3) `consumirMaterial` ahora revalida también la página del pedido e Inventario. **Sigue pendiente: no hay pruebas automatizadas** (verificación = build + Postgres local + smoke visual) — considerar smoke tests automatizados cuando el riel esté vivo.
- **Higiene de ramas (para Santiago, 1 clic c/u en GitHub → Branches):** borrar las ramas ya integradas `claude/handover-plan-continuation-tkhaey`, `claude/code-review-progress-keri4p`, `claude/dev-continuation-3d57h6`, `claude/supabase-admin-emails-pxhhsi` (todo su contenido vive en el tronco; el sandbox no tiene permiso de borrarlas). PRs abiertos: 0.

## Notas para la siguiente sesión
- **El hook `SessionStart` ya te muestra el estado y las ramas al arrancar. Léelo.** Si hay una rama `claude/*` más nueva que la tuya, reconcilia antes de codear.
- Postgres local para probar SQL: usuario `postgres` (no root) desde `/var/tmp`; stub de `auth` (roles authenticated/anon/service_role, `auth.users`, `auth.uid()`) + aplicar `supabase/migrations/00*.sql` en orden con `ON_ERROR_STOP=1`. No hacer pipe de `pgboot.sh` a `grep` o se pierde `$PSQL`.
- Capturas: `npm run build && npm run start` (sin llaves = datos de muestra) + Playwright `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`.
- Patrón firme: costo/margen/finanzas SIEMPRE en tabla aparte solo-admin (`item_costo`, `cotizacion_margen`, `pedido_costo`, `movimiento_financiero`, `proveedor`), nunca columna en la tabla principal — RLS es por fila, no por columna.
- Al crear formularios reusados en tarjetas angostas: `flex-wrap`, no `sm:flex-row` (breakpoints Tailwind son por viewport, no por contenedor).
