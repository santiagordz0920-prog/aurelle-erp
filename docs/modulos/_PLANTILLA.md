# Módulo: (nombre)

> Copiar esta plantilla al construir un módulo. Documenta lo que EXISTE, no lo planeado (lo planeado vive en el Plan Maestro). Mantener corto: es una referencia rápida, no un ensayo. Actualizar en la misma sesión en que se modifica el módulo.

## Estado
Construido en Fase X. Última modificación: (fecha).

## Tablas
- `tabla_1` — qué guarda, columnas no obvias, relaciones clave.

## Rutas / pantallas
- `/ruta` — qué muestra, para qué rol.

## Eventos que emite / consume
- Emite: (evento) → reaccionan: (módulos). Implementado en: (archivo/función).
- Consume: (evento) de (módulo).

## Lógica no obvia / trampas
- Cosas que un agente nuevo rompería sin saberlo (candados, cálculos, órdenes de operación).

## Pendientes conocidos de este módulo
- Lo que quedó explícitamente fuera y dónde está registrado.
