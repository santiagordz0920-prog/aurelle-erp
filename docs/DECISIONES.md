# DECISIONES — log técnico (append-only)

> Una línea por decisión: `fecha | decisión | por qué`. Nunca borrar; si una decisión se revierte, agregar línea nueva que la revierta. Buscar aquí ANTES de re-decidir cualquier cosa.

2026-07-05 | Stack: Next.js App Router + Supabase + Vercel | Decidido por Santiago en el Plan Maestro
2026-07-05 | WhatsApp vía Business Cloud API oficial, nunca bridges no oficiales | Riesgo de bloqueo del número
2026-07-05 | Tipografía de interfaz: Inter; fuentes de marca solo en PDFs generados server-side | Licencia de marca es desktop-only
2026-07-05 | CFDI/facturación fuera del ERP; campo folio_factura en ingresos | Lo maneja el contador externo
2026-07-05 | Esquema con sucursal_id desde día 1 | Expansión futura sin reconstrucción
2026-07-05 | Permisos vía RLS en base de datos, no solo en UI | Finanzas/márgenes solo-admin garantizado
2026-07-05 | ESTADO.md se sobreescribe; DECISIONES.md es append-only | Mantener costo de lectura por sesión mínimo
2026-07-05 | Next.js 16 + React 19 + Tailwind v4 (config CSS con @theme, sin tailwind.config.js) | Baseline de create-next-app; v4 usa tokens en CSS
2026-07-05 | shadcn/ui + cn() (clsx+tailwind-merge) + class-variance-authority + lucide-react | Sistema de componentes del plan (§5)
2026-07-05 | Rutas por área de navegación en grupo (app)/, no por tabla | Coincide con la navegación del §5 y con "trabajo real"
2026-07-05 | Roles como enum Postgres (admin/ventas/taller) + helpers SECURITY DEFINER es_admin/rol_actual/sucursal_actual | Base reusable de toda policy RLS sin recursión
2026-07-05 | Auditoría vía trigger genérico registrar_auditoria() adjunto por tabla; tabla auditoria append-only, SELECT solo-admin | Historial inmutable de quién/cuándo (§1)
2026-07-05 | Perfil de usuario se autocrea con trigger sobre auth.users; rol default 'ventas', admin se promueve a mano | Evita capturar dos veces; admin es decisión explícita
2026-07-05 | Ellion con id fijo 00000000-…-0001 como sucursal por defecto | Referenciable desde seeds y triggers
2026-07-05 | Auth en session.ts es stub (admin fijo) hasta provisionar Supabase | Permite construir y ver la UI sin nube; punto de integración documentado
2026-07-05 | zod para validación de formularios (Fase 1) | Recomendado en CONVENCIONES; estándar con TS
2026-07-05 | Interruptor supabaseConfigurado(): sin llaves usa datos de muestra + admin de prueba; con llaves va a Supabase real | Permite desarrollar y capturar pantallas sin acceso a la nube desde el sandbox
2026-07-05 | Capa de datos server-only por módulo + Server Actions con zod para escrituras | Patrón estándar Next 16 App Router; separa lectura/escritura y valida en servidor
2026-07-05 | Auth: @supabase/ssr con middleware + /login email-password; getUsuarioActual lee perfil de public.usuario | Patrón oficial Supabase para App Router; RLS sigue siendo la seguridad real
2026-07-05 | Pipeline de lead como enum estado_pipeline; cambio de etapa manual desde la ficha en v1 | El arrastre kanban y el auto-avance por eventos (Citas) llegan después
2026-07-05 | Datos de muestra de clientes viven en archivo aparte marcado como solo-local | Evita cualquier riesgo de que lleguen a producción
2026-07-05 | Costos de inventario en tabla aparte item_costo con RLS solo-admin (no columna) | RLS es por fila, no por columna; separar la tabla es la forma correcta de ocultar costo a ventas/taller
2026-07-05 | Trigger de auditoría deriva la PK del catálogo (0006), no asume columna 'id' | item_costo usa item_id; el trigger genérico de 0003 fallaba
2026-07-05 | Consignante como tabla ligera propia en Fase 1; se liga a Proveedores/CxP en Fase 2 | Inventario no debe esperar a Proveedores; buscar-o-crear por nombre
2026-07-05 | Áreas con submódulos (Taller, Ventas) usan landing con tarjetas + rutas hijas | Escala a Producción/Biblioteca/Cotizaciones/Pedidos sin romper navegación
2026-07-05 | Precio de metal: captura manual con histórico en Fase 1; API (MetalpriceAPI/GoldAPI) después | No bloquear el cotizador por elegir/pagar proveedor de API
2026-07-05 | Cotización de marca como página web imprimible (/imprimir/...), no PDF binario en v1 | Evita dep pesada + faltan archivos de fuentes de marca; browser guarda como PDF
2026-07-05 | Margen de cotización en tabla cotizacion_margen solo-admin (mismo patrón que item_costo) | Ventas ve total y líneas, nunca el costo/margen
2026-07-05 | Total de cotización se recalcula en el server action, no se confía en el cliente | Integridad del precio
2026-07-05 | Candado de anticipo 2 en pedido: override booleano admin + auditado (override_por/at) | §3.4 exige bloquear compra de materiales sin anticipo 2, con override explícito
2026-07-05 | Costo/margen real del pedido en tabla pedido_costo solo-admin; margen se sella al entregar | Mismo patrón de aislamiento de costos; margen real solo visible a admin
2026-07-05 | pago.registrado_por = auth.uid() forzado por RLS (anti-suplantación) | Trazabilidad de quién registró cada pago
2026-07-05 | Pedidos UI sigue el patrón Cotizador: data server-only + muestra + Server Actions + páginas | Consistencia; lectura/escritura separadas y validadas en servidor
2026-07-05 | Conversión cotización→pedido arranca en línea 'bridal' y se ajusta en el pedido | Un clic sin fricción; la línea es editable en el detalle
2026-07-05 | Reacciones de la matriz §4 de Pedidos quedan como comentarios TODO en actions.ts + ESTADO | Dependen de Finanzas/Producción/Postventa (Fases 2/4/6); no bloquear Fase 1
2026-07-05 | cambiarEstado('entregado') delega en entregarPedido para no saltarse el sellado de margen | El sellado debe correr sí o sí al entregar
2026-07-05 | Vínculo de tarea a entidad es polimórfico (entidad_tipo enum + entidad_id, sin FK) | Apunta a varias tablas, algunas de fases futuras; se resuelve la ruta en la UI
2026-07-05 | Tareas con RLS por sucursal (no solo-admin) | El equipo comparte pendientes; enum origen deja gancho para sugeridas por IA
2026-07-05 | Dashboard "Hoy" v1 agrega en lectura de Cotizador/Pedidos/Tareas; sin costo/margen | Pulso real sin exponer datos solo-admin; vista por rol y push llegan después
2026-07-05 | Directorio de usuarios en src/lib/data/usuarios.ts (listarUsuarios) | Reutilizable para selectores (responsable de tarea, futuros asignados)
2026-07-05 | Archivos SQL acumulativos en supabase/ (aplicar_0004_a_0008, aplicar_0005_a_0008) para SQL Editor | Santiago aplica sin CLI; 0004 ya estaba en prod, de ahí la variante
2026-07-05 | Finanzas: ledger movimiento_financiero solo-admin; monto positivo + signo por categoría | Simplifica netos/P&L y mantiene RLS solo-admin
2026-07-05 | Asiento al pagar vía trigger de BD (SECURITY DEFINER), no en el Server Action | Efecto en misma transacción; ventas registra el pago pero Finanzas es solo-admin
2026-07-05 | CxP a consignante vía trigger sobre item_inventario al reservar; monto = costo (item_costo) | Segundo evento matriz §4; SECURITY DEFINER lee costo solo-admin; guard anti-duplicado
2026-07-05 | Proveedores bajo área Dinero → tabla proveedor solo-admin; categorías como text[] libre | Consistente con Finanzas solo-admin; text[]+gin como cliente.etiquetas
2026-07-06 | Dos sesiones bifurcaron desde el mismo commit (post-"Pedidos WIP") y construyeron Pedidos/Tareas por separado; una siguió a Finanzas+Proveedores (Fase 2) | Se reconcilió adoptando la rama con Finanzas (`claude/dev-continuation-3d57h6`) como tronco de `claude/business-erp-plan-kqb9uk` vía reset+push forzado, con autorización explícita de Santiago. El esquema de Pedidos (0008) resultó idéntico entre ramas; el único conflicto de esquema real fue `tarea` (0009, dos diseños distintos). La otra implementación de Pedidos/Tareas quedó descartada del tronco pero sigue íntegra en la rama `claude/dev-continuation-3d57h6` por si hace falta consultarla.
2026-07-06 | Regla de higiene reforzada: un solo frente de trabajo por módulo a la vez | La causa raíz de la bifurcación fue tener dos sesiones activas sobre el mismo punto del repo sin coordinarse; CLAUDE.md ya lo advertía (ecosistema de documentación, regla 1) pero no había pasado en la práctica hasta ahora
2026-07-06 | Hook SessionStart (.claude/) que instala deps + imprime ESTADO y ramas paralelas al iniciar; CLAUDE.md refuerza "paso 0 anti-bifurcación" y "documentos madre obligatorios" | Convierte el protocolo de leer/actualizar docs y detectar bifurcación de "buena intención" a algo automático e imposible de saltar
2026-07-06 | Corregidos los 3 errores de lint (<a> → <Link> en cliente-form, cotizacion-builder, item-form); lint queda en cero | Higiene: dejar de arrastrar deuda conocida; navegación cliente correcta de Next
2026-07-06 | Gastos recurrentes: posteo mensual vía función SECURITY DEFINER idempotente (ultimo_posteo por mes) + Vercel Cron (vercel.json, 1° de mes) protegido con CRON_SECRET + botón manual de respaldo | §3.11; el cron no tiene sesión → cliente service_role; idempotencia evita duplicar si corre de más
2026-07-06 | Cliente service_role en src/lib/supabase/admin.ts SOLO para rutas server protegidas por secreto (crons/webhooks) | Salta RLS; jamás en navegador ni flujos de usuario
2026-07-06 | Compra a proveedor genera asiento (costo/gasto) siempre y CxP solo a crédito, vía trigger | El costo se reconoce al incurrirse; al contado no hay pasivo; evita doble conteo (la CxP pagada es el egreso)
2026-07-06 | Proyección de flujo = neto proyectado (cobros − CxP − burn·n), sin saldo de caja inicial | No trackeamos saldo bancario; honesto llamarlo flujo neto. Buckets acumulados 30/60/90; vencido y CxP sin fecha cuentan en ≤30
2026-07-06 | Comisión = % sobre utilidad real (total − costo_real), calculado en el server; requiere costo capturado | Nunca sobre el total; utilidad depende de pedido_costo (solo-admin), por eso el módulo es admin y el monto no se confía al cliente
2026-07-06 | Alta de item desde compra en el Server Action (no trigger); item_costo=monto; compra_id liga item↔compra | El alta involucra SKU/tipo que decide el usuario; la compra ya expensa el costo, item_costo es valuación de stock (sin doble conteo en P&L)
2026-07-06 | Comisiones se quedan MANUALES (sin devengo automático al entregar) | Santiago: casi no se usan; el registro manual en /dinero/comisiones sobra. No vale la pena tocar el esquema del pedido para automatizar
2026-07-06 | Fase 3: migrar el número ACTUAL de WhatsApp a la Cloud API (no número nuevo) | Continuidad de marca/clientes; se hace seguro: construir+probar el inbox en un número temporal y migrar el real AL FINAL, en ventana de bajo tráfico. Aceptado: el historial viejo no entra por la API y el número sale de la app de WA Business
2026-07-06 | Interina pre-API: mensajería ASISTIDA (wa.me con texto pre-llenado, humano envía), NUNCA bridges no oficiales | Reafirma la regla dura; con el número real en juego un bridge no oficial arriesga ban del canal de ventas. La asistida da valor sin riesgo
2026-07-06 | Foco en Fase 3; se construye primero lo que NO depende de Meta (Citas, inbox shell, envío asistido) y se enchufa el riel vivo cuando Santiago tenga verificación+WABA | El candado es externo (trámite Meta); maximizar avance construible/testeable en el sandbox mientras el trámite madura
2026-07-06 | Citas: hora anclada a Monterrey UTC-6 fijo al guardar (datetime-local + "-06:00") y display con timeZone America/Monterrey | Vercel corre en UTC; sin anclar, las horas se correrían 6h. MX sin horario de verano desde 2022
2026-07-06 | Anti doble-reserva de citas en el Server Action (traslape por sala/día), no constraint de BD | Exclusion constraint (btree_gist) es v2; el chequeo en la acción cubre el caso de una sola sucursal/2 salas
2026-07-06 | Producción: costo_produccion (capturable por taller) → trigger recomputa pedido_costo.costo_real=Σ (SECURITY DEFINER) | El taller conoce lo que paga; el margen sigue solo-admin. Recompute robusto reemplaza el costo_real manual una vez hay costos reales
2026-07-06 | Fase 4 arranca por Producción (no Documentos) | Cierra el círculo costos→costo_real→margen/comisiones que ya estaba construido; alto valor operativo para Fer; sin dependencia externa
2026-07-06 | E-firma: ruta pública /firmar/[token] con service_role (token=llave), no policies anónimas | El cliente firma sin cuenta; evita abrir RLS a anon. Firma simple (nombre+aceptación+evidencia); trazo canvas es mejora posterior
2026-07-07 | Fechas importantes (cumpleaños/aniversarios) como vista asistida SIN migración, sobre el cliente (0004) | Alto valor de fidelización usable hoy; la cadencia automática por cron espera el riel oficial de Meta. Cálculo de recurrencia anual anclado a Monterrey (mes-día, sin corrimiento de zona)
2026-07-07 | Biblioteca de media: bucket privado `media` + URLs firmadas de 1 h (no bucket público) | Una foto de la pieza de un cliente no debe quedar en una URL pública adivinable; la firma corta la genera el server
2026-07-07 | Setup de Storage (bucket + policies de storage.objects) en archivo aparte (supabase/storage/media_setup.sql), no en el glob de migraciones | El Postgres local de validación no tiene el esquema `storage`; meterlo en 00*.sql rompería la validación de la cadena
2026-07-07 | media.etiquetas como text[] con índice GIN (no tabla de tags ni enum) | Filtro simple estilo/metal/piedra para galería; lista curada en código (ETIQUETAS_MEDIA), captura flexible sin migraciones por tag nuevo
2026-07-08 | Render aprobado mueve la orden a `aprobacion_cliente` en el Server Action (alternarAprobado), solo hacia adelante | Efecto app-level tras un clic (no misma-transacción de BD); nunca retrocede etapa; registra orden_movimiento. Cierra el lazo del plan §3.5 (confirmación del cliente = etapa aprobada)
2026-07-08 | Envío de media por WhatsApp: liga firmada de 1 h embebida en el texto (asistido), no adjunto binario | El adjunto real llega con el riel oficial de Meta; mientras, la persona manda la liga que el cliente abre. Se omite la liga si es data-URI (modo muestra)
2026-07-08 | Checklist de QC: guía pre-vuelo con estado efímero (client-side), solo persiste qc_ok | Da disciplina sin migración; los ítems por línea (bridal/concierge) viven en código (QC_CHECKLIST). Editable desde la app es mejora posterior
2026-07-08 | Atasco de producción → tarea asistida (botón en la orden ≥7d), ligada al pedido con origen='sugerida' | No hay entidad 'produccion' en tarea; ligar al pedido reusa el enrutado. Cron automático es mejora posterior; el botón entrega valor ya
2026-07-08 | Contrato automático al confirmar el pedido vía crearContratoSiNoExiste (idempotente), no trigger de BD | Efecto app-level tras cambio de estado; no duplica ni pisa firmado. Reusa generarContrato. El botón manual sigue vivo
2026-07-08 | Trazo de firma en canvas guardado como dataURL PNG en documento.evidencia.firma_trazo (jsonb), acotado <200KB | Evidencia adicional; la firma legal sigue siendo nombre+aceptación+timestamp. Sin Storage extra para v1
2026-07-08 | Documentos del cliente en la ficha vía join pedido!inner por cliente_id (no columna cliente_id en documento) | documento ya liga a pedido y pedido a cliente; evita desnormalizar. Se filtra por pedido.cliente_id
2026-07-08 | Snapshot del contrato firmado: al firmar se congela ContratoDatos en documento.contenido (jsonb, migración 0022); documento firmado se renderiza de ahí, no del pedido en vivo | Integridad legal: si el pedido cambia tras la firma, el contrato firmado NO debe cambiar. Sin backfill (firmados previos siguen en vivo). /imprimir/contrato y /firmar prefieren el snapshot cuando existe
2026-07-08 | Checklist de QC editable en tabla `qc_checklist_item` (migración 0023), no en código; escritura solo-admin, lectura por sucursal; QC_CHECKLIST queda como semilla+fallback | Santiago/Fer editan los puntos sin tocar código. La orden lee puntos activos de la línea (checklistDeLinea); si vacía, cae al default. UI admin en /taller/produccion/qc
2026-07-08 | Cláusulas del contrato editables en tabla `clausula_contrato` (migración 0024), no en código; escritura solo-admin; se congelan en el snapshot al firmar | Santiago edita el texto legal sin tocar código. ContratoDatos lleva `clausulas`; construirContratoDesdePedido las incluye → el snapshot (0022) las congela. <ContratoDoc> cae a CLAUSULAS_CONTRATO_DEFAULT si faltan (snapshots viejos). UI admin en /sistema/contrato
2026-07-08 | Cron nocturno de seguimiento (0025): función SQL idempotente `generar_tareas_seguimiento` + ruta protegida por CRON_SECRET + Vercel Cron diario | Atasco de producción ≥7d y contrato sin firmar >48h → tarea automática. Idempotencia por prefijo de título + estado pendiente (no duplica cada noche; convive con el botón manual). Tarea ligada al pedido (enum entidad_tarea no tiene produccion/documento)
2026-07-08 | Riel vivo de WhatsApp: webhook receptor + envío por Cloud API + bot con IA, sin migración nueva (0018 alcanza) | El estado 'borrador_ia' de la IA vive en mensaje.estado_entrega (text). Webhook público (middleware), firma X-Hub-Signature validada, service_role para escribir sin sesión. Bot corre en after() para responder 200 rápido a Meta
2026-07-08 | Bot de WhatsApp con IA usa @anthropic-ai/sdk, modelo claude-opus-4-8 (overridable con ANTHROPIC_MODEL), structured output para clasificar+redactar | Calidad para trato al cliente; sensible (2ct+/queja/negociación/duda) → cola de aprobación (borrador_ia), seguro → envía solo. Reglas duras: nunca precios ni compromisos. Sin API key, el riel funciona sin bot
2026-07-08 | Rutas /api/webhook y /api/cron marcadas públicas en el middleware | Meta y Vercel Cron llaman sin sesión de usuario; cada una hace su propia auth (verify token/firma; Bearer CRON_SECRET). Destapó bug latente: los crons también caían en la redirección a /login
2026-07-08 | Cliente de WhatsApp casa al cliente por últimos 10 dígitos del teléfono | Meta manda 5218112345678; el cliente puede tener guardado 8112345678. Evita duplicar clientes/conversaciones por formato
2026-07-08 | Estrategia de conexión de WhatsApp: eSIM nueva (limpia, nunca en WhatsApp) a la Cloud API; el número actual del cel del negocio sigue vivo durante la transición; ads click-to-WhatsApp se re-apuntan al número nuevo al final; bot desde el día 1 | Decidido con Santiago. Cero riesgo de perder leads de ads en el corte; sin migración de número. La app/WABA debe vivir en el MISMO Business Manager de los ads para que el botón pueda elegir el número nuevo
2026-07-08 | El riel de WhatsApp (rama tkhaey, sin PR) se integró por fast-forward a claude/whatsapp-crm-integration-7fev37 y va al tronco por PR; sustituye al PR #3 (su commit viene contenido) | El tronco es lo que Vercel despliega: sin merge no hay webhook en prod y el handshake de Meta falla. Fast-forward = sin bifurcación (la rama contenía el tronco completo)
2026-07-08 | Consolidación de ramas: PR #4 (whatsapp-crm-integration) lleva TODO al tronco; PR #1 se rescató por cherry-pick (solo script promover_admins + README, sin sus docs viejos) y PR #3 se cerró como contenido; ramas claude/* ya integradas se borran | Santiago pidió "integrar todo limpio": un solo tronco desplegable antes del trámite de Meta. Los docs de julio 5 del PR #1 habrían pisado ESTADO/DECISIONES más nuevos
2026-07-08 | La app de Meta se creó SOLO con el caso de uso WhatsApp; Instagram DM + Messenger quedan en backlog como "Inbox omnicanal" (pedido por Santiago) | Llegar rápido al objetivo del día (ads→WhatsApp→CRM) sin pantallas/reviews extra; Meta permite sumar casos de uso a la misma app después. IG/Messenger necesitan webhook y matching de identidad propios
2026-07-08 | Aviso de privacidad público en /privacidad (estático, fuera del cascarón, ruta pública en el middleware) | Meta exige URL de política de privacidad para publicar la app de WhatsApp (modo Live, sin el cual no entregan webhooks de producción); además buena práctica LFPDPPP. Contacto ARCO: nubomarket@gmail.com
2026-07-08 | Conexión de WhatsApp: app creada, webhook verificado ✓ + messages suscrito, eSIM +52 2206289675 registrada en Cloud API, token permanente generado, /privacidad en prod. BLOQUEO en verificación del negocio de Meta (~2 días hábiles) antes de publicar la app y recibir mensajes reales | Meta exige verificación del negocio para entregar webhooks de producción; sin ella el Inbox no recibe aunque WhatsApp marque ✓✓. Pendiente al desbloquear: 4 env vars en Vercel + redeploy + publicar app + prueba. Checklist en GUIA_WHATSAPP.md §"Dónde nos quedamos"
2026-07-09 | Consumo de material (matriz §4 "Pieza consumida", migración 0026): función única recomputar_costo_real(pedido) = Σ costo_produccion + Σ item_costo de items consumidos; triggers en costo_produccion e item_inventario, todo SECURITY DEFINER | Cierra el círculo inventario→costo_real→margen sin exponer item_costo (solo-admin) al taller. Recompute robusto (Σ, no incremento); reserva/liberación ajustan solo. CxP a consignante NO se duplica (se crea al reservar, 0011)
2026-07-09 | QC completo → tarea sugerida "Agendar cita de entrega" (app-level en marcarQC, idempotente), no notificación push | Matriz §4 "QC completo". La tarea en /hoy es el surface hasta que exista push (Fase 5/6). El agendado real ya vive en /clientes/citas. Idempotencia por prefijo de título + pendiente (marcar/desmarcar QC no duplica)
2026-07-09 | Adelantar Fase 6 (Marketing/funnel) se pospuso: Santiago prefirió cerrar Fase 4 primero (consumo + QC→entrega). Funnel v1 queda como siguiente candidato | Cerrar el contrato §4 de la fase en curso antes de saltar de fase; higiene de "un frente por módulo"
2026-07-09 | Fase 5 paso 1: "cotización enviada sin respuesta ≥5d → tarea sugerida" se implementa AMPLIANDO el cron nocturno generar_tareas_seguimiento (migración 0027 redefine la función), no un cron nuevo | Un solo cron nocturno para todas las sugerencias; mismo patrón idempotente por prefijo de título. Proxy de "sin respuesta": estado='enviada' + updated_at ≥5d. Tarea liga a entidad_tipo='cotizacion' (prioridad media). Cierra fila §4 "Cotización enviada→Tareas"
2026-07-09 | Bug de zona horaria en "citas de hoy": el rango del día se calculaba con setHours() sobre el día UTC del server (Vercel=UTC), dejando fuera las citas de la tarde/noche de Monterrey (6pm Mty = 00:00 UTC del día siguiente). Fix: helper rangoDiaMonterrey() en src/lib/citas.ts (día de Monterrey como instantes ISO en UTC) usado en citasDeHoy() y en la agenda /clientes/citas | Reportado por Santiago (cita de hoy 6pm no aparecía en /hoy). Devuelve Z para comparar bien contra timestamptz (prod) y strings Z (muestra). MX sin DST desde 2022 → UTC-6 fijo
2026-07-09 | Resultado de cita → pipeline del cliente (matriz §4): asistio→visito, cotizo→cotizado, cerro→cerrado, SOLO hacia adelante (rango por PIPELINE_ORDEN); no-show→tarea sugerida "Reagendar" (no cambia pipeline). App-level en registrarResultado, sin migración | Columna vertebral del funnel para el arranque de ads. Solo-adelante evita que una cita vieja retroceda a un cliente ya cerrado; 'perdido' (fuera de PIPELINE_ORDEN) no se resucita solo. no-show = re-engagement sin WhatsApp (tarea); el envío llega con el riel
2026-07-09 | Alerta de stock bajo (0028): umbral por `tipo` de item en tabla umbral_stock (admin, RLS lectura por sucursal/escritura admin) + 4º bloque del cron nocturno (disponibles<minimo → tarea sin entidad, idempotente por título). UI en /taller/inventario/umbrales | v1 por tipo (dimensión estructurada); categoría fina ("solitario oro blanco") vive en texto y queda para después. Solo cuenta estado='disponible'. Tarea sin entidad porque es sobre una categoría, no un item. Sugerencia de recompra con proveedor = mejora posterior
2026-07-09 | Marketing v1 (funnel + CAC) adelantado de Fase 6 a /crecimiento (ya era el cascarón solo-admin): funnel medido POR CLIENTE agrupado por fuente_canal sobre cliente/cita/cotizacion/pedido; gasto de ads en tabla nueva gasto_publicitario (0029, solo-admin como Finanzas), CAC=gasto/cierres. Carga manual de gasto (Meta API después) | Encaja con el arranque de ads y corre sobre datos existentes. v1 por canal; por campaña/zona (fuente_detalle) es v2. Por-cliente casa con el KPI inquiry→visita del plan
2026-07-09 | Dashboard por rol (§3.16) como toggle Ventas/Taller en /hoy (?vista=taller), no por `rol`: Santiago y Fer son ambos admin, así que la vista se elige (no se deriva del rol). VistaVentas/VistaTaller cargan solo sus datos; sin migración | Separación real Santiago/Fer requeriría distinguir usuarios (hoy imposible: ambos admin). Toggle no persiste aún (default Ventas); recordar preferencia por usuario = v2. Vista Taller reusa listarOrdenes + listarUmbrales
2026-07-09 | Postventa v1 (§3.7, migración 0030): pieza_entregada (1:1 pedido) + servicio_pieza; registro creado app-level en entregarPedido (crearPiezaEntregadaSiNoExiste, idempotente por pedido_id único), garantía 12m fija, aniversarios de entrega+boda. RLS por sucursal (no solo-admin: no lleva margen). Overview en /clientes/postventa con recompra (clientes ≥2 pedidos) | Cierra fila §4 "Pedido entregado→Postventa". El ENVÍO de lifecycle (limpieza al año, aniversarios) espera el riel; hoy las oportunidades (garantías por vencer) se ven para contacto manual. Servicio.costo = ingreso al cliente, no margen secreto
2026-07-09 | Borrado manual de cliente desde la UI (solo-admin): la política RLS cliente_delete=es_admin() YA existía (0004), NO se necesitó migración. Acción eliminarCliente con guarda: si tiene pedidos, no borra (avisa) — además pedido.cliente_id es RESTRICT en BD. Botón con confirmación en la ficha (EliminarCliente) | Para limpiar clientes de prueba/duplicados. Citas y notas cascadean; cotizaciones/conversaciones/media quedan sin liga (set null). Marcar 'perdido' es la alternativa cuando sí tiene historial de ventas
2026-07-09 | Auditoría 2ª pasada: hardening de alternarAprobado (pedido_id/tipo leídos de la fila media server-side, firma reducida a (id, aprobado)), comentarios obsoletos de pagos/reserva corregidos, consumirMaterial revalida pedido+inventario | Cierra los 3 menores accionables de la auditoría del 2026-07-08. El 4º (falta de tests automatizados) queda registrado como pendiente consciente. Ramas viejas integradas listadas en ESTADO para que Santiago las borre (sandbox sin permiso)
2026-07-09 | Aceptar/descartar sugerencias (§3.15, migración 0031): descartar = columna booleana tarea.descartada (se oculta pero sigue estado='pendiente'), NO un estado 'descartada' ni delete → el cron idempotente (checa titulo+estado='pendiente') NO la recrea sin tocar el cron. Aceptar = origen sugerida→manual (app-level) | La columna booleana evita el gotcha de ALTER TYPE ADD VALUE en transacción y mantiene la idempotencia del cron intacta sin redefinir la función. Validado: descartar → cron corrida 2 crea 0
2026-07-11 | Cita agendada → tarea SUGERIDA de preparación (crearTareaPreparacion en agendarCita, app-level, sin migración): origen='sugerida' para reusar el flujo aceptar/descartar (0031); vence el día de la cita (Monterrey); idempotente por cliente pendiente sin descartar | Extensión en el espíritu de §3.15 (Santiago la pidió tras teclearla a mano para Mauricio). Sugerida (no manual) para que se acepte/descarte en un toque; una por cliente a la vez evita spam si agenda varias
2026-07-11 | Tareas recurrentes (§3.15, migración 0032): tabla plantilla tarea_recurrente (semanal/mensual, día) + columna tarea.recurrente_id; generador generar_tareas_recurrentes (función APARTE, el route del cron llama ambas por RPC) idempotente por período (date_trunc week/mes). Instancia origen='manual' | Función separada para no reescribir el cron gigante de sugerencias. recurrente_id da idempotencia precisa y traza. Dispara en el día o después (por si el cron se salta un día). UI en /hoy/tareas/recurrentes, RLS por sucursal
2026-07-11 | Postventa v2 (§3.7, migración 0033): generar_tareas_postventa (función aparte, la llama el route del cron) sugiere tareas por garantía por vencer ≤30d + aniversario de entrega/boda ≤14d. Match de aniversario por mes-día en la ventana (generate_series, sin make_date → sin bug 29-feb). Idempotente por prefijo+cliente pendiente | El envío por WhatsApp espera el riel; la tarea sugerida da la oportunidad de contacto manual ya. Ligada al cliente (entidad_tipo='cliente'); una por cliente/tipo evita spam si tiene varias piezas
2026-07-11 | Marketing v2 (§3.13): funnelPorCampana agrupa el funnel por fuente_detalle (campaña/zona) y cruza gasto.detalle↔cliente.fuente_detalle para CAC por campaña; sin migración (reusa 0029). Match por texto exacto (trim); campañas con gasto sin leads también se listan | Headline del §3.13 ("CAC por zona y por campaña"). Requiere que el texto de campaña coincida entre lead y gasto — documentado en la UI. Cumulativo (no por mes) para evitar el problema de atribución mes-a-mes; el filtro por periodo del gasto sigue disponible en la data
2026-07-11 | Expos (§3.14, migración 0034): tabla `expo` (estado+costo, RLS por sucursal) SIN tabla de leads propia — los leads del stand se guardan como `cliente` con fuente_canal='expo' y fuente_detalle=nombre de la expo; el ROI (leads→visitas→cierres→ingreso, ROI%, CAC) se calcula app-level casando fuente_detalle↔expo.nombre (mismo patrón que el funnel por campaña). /crecimiento/expos solo-admin | Reusa el pipeline y el funnel existentes en vez de duplicar tablas de leads/conversión. Match por texto exacto (no renombrar expo con leads); "visitó" = misma def de cita que Marketing para que los dos tableros cuadren. Costo no genera asiento en Finanzas (mejora posterior)
2026-07-11 | Widget flotante de Inbox global (InboxFlotante en AppShell, abajo-derecha en toda la app) + route handler /api/inbox/resumen (autenticado, RLS) con polling cada 25 s y Notification API opcional; oculto dentro del hilo (/clientes/inbox/[id]). Pendiente = no_leidos>0 o borrador_ia por aprobar | Pedido de Fer: ver/atender WhatsApp desde cualquier pantalla. Polling simple antes que Supabase Realtime (menos piezas móviles; Realtime = mejora posterior si 25 s sabe a poco
2026-07-11 | Bot: reglas anti-redacción-IA en el prompt (longitud espejo, sin muletillas de asistente ni lenguaje call-center, máx 1 pregunta, sin listas/markdown, no parafrasear, variar arranques) + CERO emojis (regla dura de Fer) con filtro RE_EMOJI defensivo sobre la salida | El trato debe ser indistinguible de una persona; el filtro cubre el caso en que el modelo desobedezca el prompt
2026-07-11 | Bot: ritmo humano al responder — marcar leído + typing indicator de Meta (indicarEscribiendoWa) y delay aleatorio (15-30 s primer contacto, 5-12 s después) antes de enviar; maxDuration=60 en el webhook para que quepa en after(). Sin typing en caso sensible (no prometer respuesta inmediata que tardará) | Respuesta a los 2 segundos delata al bot (pedido de Fer). Aleatorio para no tener ritmo de máquina
2026-07-11 | Identidad del bot: si preguntan "¿con quién hablo?/¿eres bot?" → sensible=true y responde un humano; el bot no afirma ni niega ser IA y no se inventa nombre | Ni mentirle al cliente ni delatar el sistema: la pregunta la resuelve una persona real, que sí puede firmar como ella misma
2026-07-11 | Spec de follow-ups/cadencias de Santiago guardado en docs/SPEC_FOLLOWUPS.md con anotaciones de ingeniería. Decisión ancla: NO habrá tabla `lead` paralela — `cliente` ya es el lead (el riel lo crea al primer mensaje y ya cuelga citas/cotizaciones/conversación); la máquina de estados extiende el pipeline existente + tabla de plantillas de cadencia | Duplicar la entidad lead separaría los datos del mismo humano en dos tablas y repetiría el error que Expos evitó (0034). El T1 "<15 min" ya lo cumple el bot del riel
2026-07-11 | Sync de gasto Meta Ads (Marketing v3, sin migración): lib/meta-ads.ts (insights level=campaign, this_month+last_month) + ads-sync.ts con upsert app-level por (periodo, canal='ads', detalle=campaña) — si la fila existe se ACTUALIZA el monto (la API es fuente de verdad de ads; lo manual queda para expo/referido) + cron diario /api/cron/ads + botón manual en /crecimiento | Sin unique index nuevo (cron mono-hilo, llave natural suficiente) = sin SQL pendiente para Santiago. Activa solo con META_ADS_ACCESS_TOKEN (System User + ads_read) y META_AD_ACCOUNT_ID en Vercel; sin ellas el cron responde ok con nota. Conversions API (eventos cita/cierre hacia Meta) queda para después del módulo de follow-ups
2026-07-11 | Bot: tuteo SIEMPRE (nunca usted, aunque el cliente hable de usted) como regla explícita del prompt, por encima del espejo de registro formal/casual | Pedido de Fer: el usted suena a banco, no al trato Aurelle; el espejo de formalidad se mantiene en vocabulario, no en el pronombre
2026-07-11 | Bot: iteración de tono con mensajes reales de prod — reglas nuevas: prohibido el comodín vago en preguntas ("¿o algo distinto?": opciones concretas o pregunta abierta) y el lenguaje de catálogo ("desde X hasta Y", "contamos con"); se agregó sección EJEMPLOS DE TONO (bien/mal) al prompt como few-shot | Primer caso real reportado por Fer ("busco ver diseños"). Patrón de trabajo: cada respuesta que rechine se convierte en regla o ejemplo, no en parche puntual
2026-07-11 | Bot: vetado "te late" y coloquialismos de compa; invitar con "¿te gustaría...?" / "si quieres..." variando (regla + ejemplo bien/mal en el prompt) | Segundo caso real de Fer en prod. "Te late" es demasiado informal para marca premium; se pide variar las dos formas permitidas para que no se vuelvan plantilla detectable
2026-07-11 | Bot con hechos fijos del negocio en el prompt (Plaza Ellion/Gómez Morín/San Pedro + link Maps + horario 10-20) tras alucinar "showroom Ellion, Monterrey"; vetado "te acomoda" | Tercer caso real de Fer. Un bot de negocio necesita sus datos duros EN el prompt, no en el conocimiento del modelo; regla del spec de follow-ups adoptada ya: "nuestro showroom", nunca la plaza como marca
2026-07-11 | Bot propone horarios reales (0035): slots de agenda calculados server-side (citas piso_ventas + sugerencias vigentes) inyectados como contexto AGENDA + salida estructurada horario_sugerido (enum restringido) + apartado 24h con unique(sala,inicio) — dos chats simultáneos NUNCA reciben el mismo horario (el 2º insert falla y ese chat ofrece otro slot) | Pedido de Fer: el default al invitar es horario concreto disponible, no "¿qué día puedes?". El bot solo propone, NO crea la cita (bot v2). Colisión validada en Postgres local; sin 0035 en prod degrada con gracia (sin candado, disponibilidad real sí)
2026-07-11 | Inbox con AutoRefresh (router.refresh cada 5 s en el hilo / 10 s en la lista, solo pestaña visible) en vez de Supabase Realtime | Fer recargaba a mano para ver mensajes nuevos. refresh() conserva estado del cliente; Realtime queda como upgrade si el polling sabe a poco
2026-07-11 | Horarios del bot por RANKING, no cronológicos (propuso sábado 7 pm): puntajeSlot con franjas doradas 11-13/16-18, castigo a 7 pm y fines de semana de noche, bonus por cercanía; alternativa siempre de otro día. Pesos en constantes comentadas | Acordado con Fer: priorizar horarios cómodos de atender Y probables para el cliente. Iterar pesos con datos reales de resultado de cita por franja cuando haya volumen
2026-07-11 | Vetado "aquí la ubicación" (variantes naturales: "es esta la ubicación:") y horas SIEMPRE am/pm, nunca 24 h | Cuarto ajuste de tono con mensajes reales de prod
2026-07-11 | Human-in-the-loop de horarios: toda respuesta del bot con propuesta de horario va a la cola de borradores (constante HORARIOS_REQUIEREN_APROBACION=true en bot-whatsapp.ts; el slot se aparta igual mientras se aprueba) + aprobación at-a-glance en el widget flotante (texto del borrador + Aprobar/Descartar inline, reusando las acciones del Inbox) | Fer no confía aún en el ranking de horarios: los socios revisan cada propuesta desde cualquier pantalla en dos toques. Cuando el ranking esté calibrado, false = envío directo. No se creó pantalla nueva de aprobaciones: el widget YA es esa lista
2026-07-11 | Bugfix bot: con borrador_ia pendiente en el hilo, el bot NO ofrece AGENDA (enum fuerza horario_sugerido="ninguno") y recibe el borrador como contexto interno ("no lo repitas, no inventes horarios, si insisten en agendar di que en un momento confirmas, sin sensible=true solo por eso") | Bug del human-in-the-loop: si nadie aprobaba el horario, cada mensaje nuevo del cliente generaba OTRO borrador con horario y el bot enmudecía; ahora sigue conversando mientras el borrador espera aprobación
2026-07-11 | Intake multicanal en cliente (0036): correo/instagram/facebook/otro_contacto + contacto_preferido (check en BD); "mínimo un contacto" se valida en zod (validarContacto compartida alta/edición/expo), NO con CHECK | Hay clientes históricos solo-nombre: un CHECK les bloquearía cualquier update. El preferido es el ÚNICO contacto que muestran las listas; los demás solo quedan en la ficha
2026-07-11 | Teléfonos SIEMPRE sin espacios/guiones (normalizarTelefono en zod + riel + expo; 0036 normaliza existentes fila por fila con manejo de colisión) y búsqueda de /clientes por solo-dígitos cuando el query trae ≥4 dígitos | WhatsApp copia los números con espacios; sin normalizar, la búsqueda y el match por teléfono fallaban. Colisión de duplicados reales se conserva sin normalizar (se resuelve a mano)
2026-07-11 | Lista de leads at-a-glance en /clientes: listarLeads() junta cliente + su conversación más reciente (ultima interacción, no_leidos) + borradores IA; chips de filtro por etapa con conteo; EstadoSelectorMini = override manual de etapa en la fila | Feedback de Santiago: ver de un vistazo qué busca cada lead, quién necesita atención (sin contestar / IA por aprobar) y que Fer mueva etapas sin abrir la ficha
2026-07-11 | Ficha viva: el bot devuelve resumen_interes y nombre_cliente en su structured output y actualizarFichaLead los persiste en cliente.interes/nombre con cada mensaje | Feedback de Santiago: saber qué busca el lead sin leer el hilo, y que el nombre real reemplace al alias de WhatsApp cuando el cliente lo dice. Guardas: nunca el nombre de la pareja; no degradar "Ana García" a "Ana"
2026-07-11 | Bot con borrador pendiente v2: la conversación sigue completamente normal (info, preguntas); lo ÚNICO en pausa es proponer horario | Ajuste de Santiago sobre el fix del mismo día: el bot debe seguir dando información mientras espera la aprobación del horario
2026-07-11 | Contactos útiles (0036): tabla contacto_util propia en /taller/contactos, visible para TODOS los usuarios (RLS por sucursal, no solo-admin), tipo con check (joyero/vaciador/montador/otro) y todo lo demás texto libre opcional | Pedido de Santiago: directorio "a la vista" para tercerizar procesos; los precios son estimados informales de terceros, no finanzas/márgenes de Aurelle (la regla solo-admin no aplica). Distinto de proveedor (0012): comprar vs. encargar
2026-07-12 | Bifurcación de "contactos útiles" reconciliada: dos sesiones construyeron el mismo módulo en paralelo (la sesión CRM se colgó y Santiago relanzó el pedido en otra); se adopta la versión ya MERGEADA del tronco (0036_contactos_utiles, PR #39), la duplicada se elimina y la migración CRM/lead se renumera a 0037_lead_interes_telefonos | Regla de higiene: nunca reescribir historia mergeada + un solo frente por módulo. El SQL de contactos válido es el 0036 del tronco
2026-07-12 | Plantillas de Meta tanda 1 redactadas (docs/PLANTILLAS_META.md, fuente de verdad): 3 UTILITY (recordatorio cita mañana/hoy, pieza lista) + 2 MARKETING (seguimiento visita, reactivación), es_MX, manual de estilo del bot; el alta en Meta la hace Fer con una sesión de Cowork | Texto libre fuera de la ventana de 24 h es rechazado por la API (no es toreable variando redacción): plantillas son la única puerta. UTILITY=transaccional del cliente, MARKETING=seguimiento; variantes y botones de respuesta rápida = v2
2026-07-12 | Diseño completo del funnel de follow-ups en docs/DISENO_FUNNEL_MENSAJES.md (sustituye al xlsx inexistente): principio "la plantilla no vende, reabre la puerta; cierra el bot", 6 arquetipos de mensaje con 2-3 variantes c/u, cadencias por estado, tabla `toque` para instrumentar TODO envío (respuesta/cita/cierre atribuidos), informe semanal de IA con ranking de plantillas + curva de mortalidad + borradores de variantes nuevas, A/B con mínimo 30 envíos por variante y un cambio a la vez | Pedido de Fer: funnel super fuerte + el agente mide qué funciona y da pointers de mejora. La instrumentación se diseña ANTES que las plantillas porque define el esquema. Construcción en 4 fases (F1 semiauto → F4 auto gradual)
2026-07-12 | Decisiones del funnel: tiempos OK; incentivos por fase REQUIEREN sesión aparte (no redactar arquetipo INCENTIVO aún); F2 auto = solo recordatorios+pieza lista; informe semanal por WhatsApp 1:1 a Santiago y Fer, mega corto (la Cloud API NO puede escribir en grupos — el plan del grupo NuboMarket no aplica con el número de la API) | Respuestas de Fer a §7 del diseño
2026-07-12 | Simulador de leads (§8 del diseño, requisito de Fer para salir en vivo): mismo código real (riel+bot+cadencias) con transporte capturado (es_simulacion no llama a Meta), reloj virtual (motor de cadencias recibe `ahora`), leads actuados por otra IA con biblioteca de personas, asserts automáticos por escenario y transcripts legibles en el Inbox con etiqueta SIMULACIÓN | "Demostrar que un lead de dentro de 6 meses funcionará perfecto, en minutos" — F1 y simulador se construyen juntos; criterio de salida: batería verde 2 corridas + socios leyeron transcripts
2026-07-12 | Plan de migración de sheets en docs/MIGRACION_DATOS.md: 3 fases (escaneo→paquete con INVENTARIO/MAPEO/FALTANTES.md + CSVs · conciliación con socios · import por Claude Code) y 9 principios (nunca inventar datos: hueco=pregunta; dedup por teléfono contra clientes del riel; verificación por sumas centavo a centavo; origen='migracion'+lote reversible e idempotente; dry-run local antes de prod con OK de Santiago; histórico no dispara eventos §4) | Fer anticipa datos incompletos y conciliaciones "pesadilla": el diseño asume huecos como ciudadanos de primera clase. Drive ya accesible desde las sesiones (sheets identificadas por ID)
2026-07-13 | Paquete de migración (Fase 1, escaneo hecho en Cowork por Santiago) adoptado y versionado en docs/migracion/ (INVENTARIO/MAPEO/FALTANTES/LEEME + 8 CSV en csv/); el zip suelto de la raíz se eliminó del repo. Verificado independientemente: sumas de control cuadran exactas (Ingreso $2,257,368 · Gasto $6,173,822 · Deuda $4,289,840 · PagoDeuda $445,271 · pedidos $1,825,270) | El paquete es el insumo de las fases 2-3; versionarlo da trazabilidad y las próximas sesiones lo leen sin depender del zip ni del chat
2026-07-13 | Conciliación estructurada en docs/migracion/CONCILIACION.md: las 31 preguntas de FALTANTES reordenadas en 4 bloques por qué desbloquean (A reglas del import → B dinero vivo ~$464k saldos → C inventario/proveedores → D no bloquea), con línea R: por pregunta y contador de avance; FALTANTES.md queda intacto como referencia de detalle | Los socios contestan en orden de impacto y el import puede arrancar con A contestado sin esperar las 31; pre-macheo de F-12/F-06 ofrecido como atajo para acortar la sesión B
2026-07-13 | Conciliación de migración contestada por Santiago en sesión interactiva (29/31; F-15/F-16 a Fer, no bloquean): Contabilidad = source of truth de pagos (Producción referencia suave, ni Ventas ni Producción completas, sin cuadre 1:1); F-23 como propuesto (deuda/pago_deuda/capital = tipos nuevos en Finanzas, migración SQL nueva); MON EXPO=disponible; A9 apartado con saldo $18,500 (gana Producción); Heber=pedido cancelado con reembolso; costos de Ventas NO se ligan a pedidos (históricos sin costo real) ni se corrigen las celdas rojas (data no existe); A101/103/104/106 resueltos por cruce de certificado = piedras del maestro montadas (no se importan aparte) | Respuestas completas y acciones derivadas en docs/migracion/CONCILIACION.md — es el checklist de la Fase 3
2026-07-13 | Correcciones de catálogo salidas de la conciliación: María de Lourdes Pérez Lozano es CLIENTE (no consignante); "Tío Fer" no es ni cliente ni consignante (el batch Ar01–Ar126 es de La Jefa/mamá de Fer y "Precio Tio Fer" = lo que se devuelve al venderse); Forte = cajas/empaque (no diamantes); +proveedor Wholesaler (diamantes y churumbelas); Churumbelas Ch1–Ch11 SÍ es inventario físico y se agrega al alcance del import (MURIO = pieza dañada/perdida = baja) | Santiago corrigió las inferencias del escaneo; los CSV del paquete se ajustan en la Fase 3 sin tocar los originales
2026-07-13 | Pre-macheo de Fase 3 generado en docs/migracion/PREMACHEO.md (F-12: 82 ingresos ligados único/26 ambiguos/34 sin match; F-10: tabla saldos vs ingresos con 5 casos a revisar; F-14: 36/44 gemas en el maestro y 8 sin match a decidir). HALLAZGO: 29 "ingresos" son transferencias internas ($388,051, 17%) — propuesta: tipo transferencia_interna fuera del P&L, pendiente OK de socios | La lectura de Drive solo devuelve la primera pestaña del libro: churumbelas Ch1-Ch11 se extraen vía Cowork o pegadas por Santiago
2026-07-13 | Resoluciones post-premacheo de Santiago: transferencias internas ($388k) = tipo transferencia_interna fuera del P&L; las 8 gemas sin match = piezas reales (alta con costo pendiente); Mauricio dic-25=Treviño, Luna=Luna Hermano, Sara LOTR=Sara, movs "papá fer"=financiamiento familiar, José Sandoval=cliente histórico nuevo; Jonathan ($84k) y Juan ($100k, un solo Juan prob. Vega) se REPARTEN POR CUADRE contra sus pedidos (regla aprobada: asignar por monto/fecha hasta que cada pedido llegue a su precio); Jorges/Gerardo/Marcos Reyes + ~30 sin-match quedan para Fer y entran sin ligar | Detalle en docs/migracion/PREMACHEO.md §Resoluciones — con esto el import de pagos ya tiene reglas completas
2026-07-13 | Fase 3 del import construida: migración 0038 (categoría transferencia_interna signo 0 fuera del P&L + migracion_lote/migracion_registro solo-admin + revertir_lote_migracion SECURITY DEFINER) y generador scripts/migracion/ (resoluciones.mjs = decisiones como datos; generar-import.mjs emite SQL de una transacción, un DO-block por fila, idempotente por llave natural; out/ gitignoreado). trg_pago_asiento se deshabilita durante el import porque el ledger viene completo de Contabilidad (evita duplicar ingresos); histórico no dispara eventos §4 | Dry-run local VERDE: sumas centavo a centavo vs controles, re-run sin duplicar, revert a cero, dedup por teléfono contra clientes del riel (creado=false sobrevive al revert). Detalle en docs/modulos/migracion-import.md
2026-07-13 | Rastro de import por tabla puente (migracion_registro con llave natural única) y NO columnas origen/lote en cada tabla de negocio: revert = DELETE por lote en orden inverso de FKs, las hijas (item_costo/pedido_costo/cotizacion_linea/nota_cliente) caen por cascada del padre; la descripción de la pieza de cada pedido histórico vive en una cotización 'aceptada' creada por el import (pedido no tiene columna de descripción) | Evita tocar el esquema de negocio y deja el import 100% reversible sin migraciones extra
